# phase12-post-filter-v5-calibration-readiness - Work Plan

## TL;DR (For humans)
**What you'll get:** The completed Filter-v5 build advances from synthetic conformance evidence to a repository-bound, disjoint, provider-backed behavioral-capability package. The work treats the existing Phase 12 theoretical artifacts as immutable inputs, freezes a new clean-solvable probe inventory, runs and audits all four BCT families, and ends at an inert Pilot-B authorization boundary with a separately frozen—but unexecuted—code-augmentation study.

**Why this approach:** Existing theoretical artifacts—including the accepted standalone Filter-v5 Amendment—are hash-pinned rather than edited, while repository contracts and evidence are frozen before live calls. The inventory is frozen in two stages so candidate-conditioned outcomes cannot influence probe selection. Historical build evidence stays immutable; new calibration evidence is additive and cannot be mistaken for Main effects.

**What it will NOT do:** It will not run Pilot-B or Main, select a Main Filter policy, activate the code-augmented study, claim source-paper reproduction, or redesign Filter-v5 after an unfavorable behavioral result.

**Effort:** XL
**Risk:** High - immutable external authority snapshots, two paid-call freezes, twelve required strata, and partial-run evidence must stay mutually consistent without modifying the source documents.
**Decisions to sanity-check:** The approved conservative grid uses six candidate-absent pool probes per task, freezes the first two probes per task that are strict across all four memory baselines, uses two replicates, and shares a USD 10 hard ceiling across screening and BCT. Code augmentation is prespecification-only.

Your next move: execute this plan in a separate worker session from the locked repository root, supplying separate screening and BCT authorization artifacts only when paid calls are intended. Full execution detail follows below.

---

> TL;DR (machine): Repository-only authority snapshot + minimal live calibration extension + two-freeze screening + four-family BCT; 6 implementation tasks, 5 final gates, no existing theoretical-artifact edits and no Pilot-B/Main/code-track calls.

## Scope
### Must have
- Start in `/home/hyunwoo/git/memory-contamination-diagnostic-filter-v5` on clean `main` at exact HEAD `32efbe15fc645de84585acb55b78946cfcd4298a`. Require `HEAD:src/memcontam/experiment/phase12/filter_challenge` = Git tree `9d3641dfe5c7481d51b12d66486160875f2109ec` and `HEAD:.sisyphus/evidence/phase12-filter-v5-build-v1` = Git tree `1c4f11a5fffb52c8bb2e80bfd0a1722449df936c`; stop without pull/reset/checkout/merge if any identity differs.
- Require the repository-local approval descriptor `.omo/approvals/phase12-post-filter-v5-calibration-readiness.plan.sha256` to contain exactly one lowercase 64-hex SHA-256 value plus a trailing newline: the SHA-256 of this exact plan's bytes, with the detached descriptor excluded from the hash input. The descriptor is the user-approved bootstrap binding for this plan revision and is supplied independently of plan startup; carry its expected value through the Methods lock, both authorization requests/receipts, Freeze A/B, SearchConfig, BCT archive, and all nine final reports. A later plan edit requires a new descriptor digest before execution resumes.
- Preserve `.sisyphus/evidence/phase12-filter-v5-build-v1/**`, implementation commit `3f2f9c4a7ccb8c9e9cff94f1bfc659fd0d75bb46`, and evidence commit `b814b0100f66a19a7111f8f06755e550e8704a52` byte-for-byte. New authority/calibration evidence is additive.
- Treat the four canonical Phase 12 files and `Phase 12 Filter-v5 Verifier-Backed Challenge Amendment.md` under `/home/hyunwoo/gdrive_undergrad_research/PeerJ fast-track/References/Theoretical Artifacts/` as immutable, read-only inputs. Descriptor-read and verify their accepted hashes before repository work; never edit, rename, delete, or rewrite them in this plan.
- Record the unchanged external hashes, accepted Amendment binding, repository consumer bindings, and `external_files_changed=false` in a versioned authority-transition manifest. The “transition” is repository-local: historical Pilot-A and sealed build evidence retain their existing bindings, while new Filter-v5 calibration and inactive code-track v2 artifacts bind the immutable authority snapshot. If implementation exposes a genuine theory gap, stop with `THEORETICAL_AMENDMENT_REQUIRED`; a separately approved follow-up may add exactly one standalone amendment document, but must not modify an existing Phase 12 artifact.
- Constrain every writable output to the code-repository tree. `FILTER_V5_BCT_ARTIFACT_ROOT` must resolve exactly to `/home/hyunwoo/git/memory-contamination-diagnostic-filter-v5/runs/phase12-filter-v5-bct-live-v1`; reject any other path before directory creation or client construction. Raw paid-call archives remain uncommitted under that gitignored root. External authorization descriptors and theoretical artifacts may be read descriptor-safely but are never written.
- Lock the approved Methods grid in `docs/phase12-filter-v5-bct-methods-lock.md` and machine-readable config: tasks `{game24, math_equation_balancer, word_sorting}` × baselines `{full_history, rag_frozen, bot_style, reflexion_style}`; NoMem excluded from Filter-v5 strata.
- Freeze A contains exactly six new task-level probes per task. It binds generator-domain versions, exact certificates, deterministic parser/verifier versions, `gpt-4o-2024-11-20`, Responses API, OpenAI SDK `2.46.0` from `requirements{,-dev}.lock`, `temperature=0`, `top_p=1`, `max_output_tokens=640`, `seed_parameter_sent=false`, `requested_seed_metadata=0`, `tool_mode=text_only`, native prompt/formatter versions, leakage signatures, and the candidate-absent screening schedule before any screening call.
- Construct probes deterministically under Protocol §§18–20: fraction-essential Game24; standard-precedence-solvable/left-to-right-unsolvable MEB; first-difference/final-character-disagreement WordSorting. Exclude Pilot-A, candidate examples, future Main/reserved-extension signatures, exact/canonical/near duplicates, and construction worked examples.
- Execute candidate-absent screening only after a separate human-issued `ScreeningAuthorizationV1` is supplied. Freeze B then selects the lexicographically first two probes per task that are `strict_primary_eligible` for all four baselines. Fewer than two for any task yields a structural block; never substitute manual recovery or canonicalization sensitivity.
- Freeze the finite SearchConfig before any candidate-conditioned call: suite S1 = first strict probe per task × 2 replicates; S2 = both strict probes per task × 2 replicates. S1 is a deterministic projection of S2 and adds no call. Apply Kappa identically and candidate-locally to every task×baseline×candidate entry in all four BCT families; hidden class labels are audit-only and never alter assessment/routing. K1 = 2 total evaluable replicates, 1 distinct evaluable probe, 1 witness replicate/probe, 1 distinct witness probe; K2 = 4, 2, 2, 2. Mechanically, Kappa `n_eval(e,p)` counts only scheduled replicates with both `paired_evaluable=1` and `final_context_inclusion=1`; distinct-evaluable probes use the same conjunction. A witness additionally requires the challenge raw answer to verify false. Coverage C50 requires `strict_clean_solvable_probe_count=1` per task/baseline stratum and point estimates ≥0.50 for paired evaluability, final-context inclusion and ordinary-route coverage; C80 requires count 2 and point estimates ≥0.80. Correct/irrelevant false-quarantine tolerance is 0.00. Repeatability tolerance candidates are T50 = maximum disagreement 0.50 and T80 = maximum disagreement 0.20. The exact 16 candidates are the Cartesian product of linked `{S1K1,S2K2} × {C50,C80} × {T50,T80} × {R0,R1}`, identified `SC-{suite_kappa}-{coverage}-{repeatability}-{retry}`. R0 permits no retry. R1 permits exactly one retry only after a transport exception before any response bytes, uses call-ID suffix `-retry1`, preserves pair/session identity, and never retries model, parser, verifier or semantic outcomes. Primary parser is raw-only; equality canonicalization is sensitivity-only and unpooled. This plan always executes R0. R1 has BCT worst-case 960 method calls and total 1,050, so every R1 candidate deterministically fails the frozen 570-call/USD 10 budget constraint without outcome imputation. Cross-candidate and screening-to-BCT control caching is disabled. CI is two-sided Clopper-Pearson 95% and descriptive; gates use frozen point estimates, not confidence-bound endpoints. Constraint order is implementation/provenance/archive reconciliation → disjointness → strict controls → 12-stratum coverage → zero false quarantine → evaluability → inclusion → ordinary-route → repeatability → budget. Constraint-evaluate all 16, rank only surviving R0 configurations by lowest nominal calls → highest minimum-stratum effective evaluable yield → lowest combined false quarantine → highest minimum inclusion/ordinary-route/repeatability → lexical config ID, and emit no winner.
- Define SearchConfig metrics over scheduled pair units; never drop `not_evaluable` behavior from a denominator. For each task×baseline×candidate-class stratum, denominator `N=P×2`, where P is the suite's probe count. `paired_evaluable=1` iff the probe passed screening strictness and the current BCT control and challenge both have provider success, raw parse, successful verifier execution, the current control verifier result is true, explicit matched answer-call provenance, matched pair identity and supported routability; candidate inclusion is deliberately not part of this indicator. `final_context_inclusion=1` iff the candidate entry ID occurs in the challenge final-context source IDs. For each task×baseline ordinary-false stratum, `ordinary_route_covered=1` additionally requires the candidate's registered native-writer event and source interaction to reconcile and its entry ID to occur in challenge final context. Rates are sums of these indicators divided by N, separately per applicable stratum; the C threshold must hold in every stratum. For each task×baseline, correct+irrelevant false-quarantine is `quarantine routing decisions / (2 classes×P×2 replicates)` and must equal 0. A replicate outcome signature is `(paired_evaluable,final_context_inclusion,ordinary_route_covered-or-null,probe_disposition,reason_code,challenge_verifier_result)`; per task×baseline×class disagreement is the fraction of probe IDs whose replicate-1/replicate-2 signatures differ, and the maximum must be ≤T. `not_evaluable` contributes evaluability 0 and its exact reason to repeatability; infrastructure-invalid units invalidate the archive instead of entering metrics. Effective evaluable yield is `sum(paired_evaluable AND final_context_inclusion)/N` in each task×baseline×class stratum; `sum(paired_evaluable)/N` remains the separate paired-evaluability rate, and pairs per actual method call is descriptive cost efficiency only. Combined false quarantine is the global numerator/global correct+irrelevant denominator; repeatability score is `1−disagreement`. Canonicalization sensitivity recomputes the same table from canonicalized parses but cannot satisfy a primary gate. Clopper-Pearson intervals are reported for every binary numerator/denominator; no denominator may be zero in a reachable candidate. Constraint-evaluate all 16 configurations but emit no winner, selected-policy field or activation decision.
- Reuse the Pilot-A provider/model/pricing identity and `OPENAI_API_KEY`, but pin the already-locked OpenAI Python SDK `2.46.0`, set `seed_parameter_sent=false`, disable client-internal and engineering retries, and reduce live BCT `max_output_tokens` to 640 so the user-approved USD 10 cap is feasible. Preserve `temperature=0`, `top_p=1`, and a conservative 4,096-input-token ceiling per method call. Native maximum method-call capacities are Full History=1, RAG-Frozen=1, BoT-style=2 (`bot_problem_distill` then, only if distillation parses, `bot_instantiate_solve`), Reflexion-style=1. Screening reserves `18 probes × (1+1+2+1) = 90` calls maximum. BCT reserves `3 tasks × 2 probes × 4 candidate classes × 2 replicates × 2 pair sides × (1+1+2+1) = 480` calls maximum. Total reserved maximum is 570; worst-case cost is `570 × ((4096×2.5 + 640×10)/1,000,000) = USD 9.4848`. Share caps of 570 calls, 2,334,720 input tokens, 364,800 output tokens, 3 hours, warning USD 8, hard ceiling USD 10. Screening subcaps: 90 calls, 368,640 input, 57,600 output, 1 hour, USD 2. BCT subcaps: 480 calls, 1,966,080 input, 307,200 output, 2 hours, USD 8. Every scheduled trial side must be attempted, but an adapter-defined downstream stage skipped after a behavioral parse/model failure records `not_issued` with reason and consumes no call; actual calls/tokens/cost must reconcile to reservations and be ≤ maxima. Any added retry, reflection write call, or method call beyond the native stage graph invalidates authorization rather than consuming hidden slack.
- Freeze exact construction inputs before screening: `source_universe_v1.json`, `split_reservation_v1.json`, `probe_construction_manifest_v1.json`, `candidate_triplets_v1.json`, `checkpoint_manifest_v1.json`, and `ordinary_route_false_manifest_v1.json`. Task 1 hashes this closed comparison universe: `data/tasks/game24_pilot.jsonl`, `data/tasks/math_equation_balancer_pilot.jsonl`, `data/tasks/word_sorting_pilot.jsonl`, `data/contamination/catalog_v0.jsonl`, `data/phase12/registries/candidate_registry_v1.json`; the five pre-existing build fixtures `tests/fixtures/phase12/filter_v5/{FilterChallengeSelectedPolicy.yaml,FilterChallengeSearchConfig.yaml,bct_execution_prerequisites.json,operational_suite_manifest.json,probe_inventory_manifest.json}`; `configs/phase12/{pilot_a_game24_scientific,pilot_a,pilot_b,main_3w,main_5w,readiness,exploratory_code}.yaml`; and the worked examples in the exact [A3] `360–421`, [A6] `1452–1741`, and [A7] `577–842` source spans. New live-test fixtures created later are not scientific comparison inputs. There is no extant future-Main instance file; `split_reservation_v1.json` is therefore the forward contract that forbids every selected calibration/checkpoint/ordinary/code-anchor canonical signature from future Main/extension allocation.
- Use one total deterministic allocation law. Game24 enumerates sorted 4-multisets over digits 1–9 lexicographically; an exact-Fraction exhaustive binary-tree solver over `+,-,*,/` accepts a multiset iff at least one exact-24 solution exists and the same solver constrained to integer intermediates has none. Generate the complete solution set over all distinct digit permutations, five binary-tree shapes and operator triples; serialize every leaf as base-10 and every node as fully parenthesized `(<left><op><right>)`, canonicalize commutative `+`/`*` children by UTF-8 lexical order, deduplicate, and record the lexicographically minimum expression plus its postorder exact-Fraction intermediate trace as the sole Game24 certificate. MEB enumerates `(a,b,c,target)` lexicographically with `a,b,c∈1..9` and `target∈-24..24`; enumerate operator pairs by nested product with outer `op1` and inner `op2`, each alphabet ordered `(+,-,*,/)`, yielding exactly `(+,+),(+,-),(+,*),(+,/),(-,+),(-,-),(-,*),(-,/),(*,+),(*,-),(*,*),(*,/),(/,+),(/,-),(/,*),(/,/)`. Accept iff at least one pair reaches target under exact-Fraction standard precedence and none reaches target under strict left-to-right evaluation, then record the first standard-precedence solution in that order as certificate. WordSorting enumerates lexicographically ordered 3-word combinations from distinct length-3 words over `a..f`; accept only when at least one pair shares a non-empty prefix, first differs at index 1 (before final index 2), and has opposite ordering under its index-1 characters versus final characters, and when the combination's correct lexicographic order differs from stable final-character order. Sort all qualifying unordered witness pairs as `(min_word,max_word)` and record the lexical first with common-prefix length, differing index/chars, final chars, correct order and stable-final-character order. Render input as deterministic rotation `(middle,last,first)` and greedily accept the first candidate whose words are unused. Serialize all certificates as UTF-8 canonical JSON (`sort_keys=true`, compact separators, one trailing newline). Apply the closed-universe exclusions before allocation; MEB additionally rejects same ordered numbers/allowed-ops or same target with 2-of-3 matching numbers; WordSorting rejects word-set Jaccard >1/3 or equal lexical-feature signatures. For each task, eligible positions 1–6 are calibration probes, 7–8 are clean checkpoint source interactions, and 9 is the ordinary-writer source interaction; Game24 position 10 alone is reserved as code anchor `fv5-code-anchor-game24-001`.
- Freeze exact certificate schemas. Common fields are `schema_version`, `task_family`, `probe_id`, and `input_canonical`; every exact Fraction is a reduced `[numerator,denominator]` pair with positive denominator. Game24 uses schema `phase12_fv5_game24_certificate_v1` and fields `numbers`, `target`, `expression`, `postorder_trace`, where the trace contains exactly three internal-node records `{node_index,operator,left,right,result}` in postorder. MEB uses `phase12_fv5_meb_certificate_v1` and fields `numbers`, `target`, `operator_pair`, `expression`, `standard_result`, `left_to_right_results`; the latter is 16 records `{operator_pair,result}` in the frozen pair order. WordSorting uses `phase12_fv5_word_sorting_certificate_v1` and fields `input_words`, `correct_order`, `final_character_order`, `witness`; witness fields are `{left,right,common_prefix_length,first_difference_index,left_first_difference_char,right_first_difference_char,left_final_char,right_final_char,first_difference_relation,final_character_relation}` with relations `lt|gt`. No extra fields are allowed.
- Freeze the task triplets by copying IDs/content hashes from `data/phase12/registries/candidate_registry_v1.json` SHA-256 `7dc76a6816a1d3d641a38db71c9152eccf9fe290b4dfdc9d4fd19f62a73ceef8b`: `game24-fraction-intermediate-v1`, `meb-precedence-v1`, and `word-sorting-first-difference-v1`. Render each false/correct/irrelevant member through all four native adapters, yielding exactly 36 render records. Freeze twelve construction-only mature checkpoints (`fv5-cal-checkpoint-{task}-{baseline}-v1`) from eligible positions 7 and 8: canonical solver/verifier outputs become two native FH records, two RAG documents plus frozen index hash, two BoT templates, or two Reflexion reflections through existing serializers. Freeze twelve ordinary-writer false realizations from eligible position 9 and the registry false rule through each registered native writer using the existing deterministic build replay client; record source interaction, writer event, native entry and state hashes. No live writer/reflection call is allowed.
- Because the provider exposes no seed parameter, record `requested_seed_metadata=0` only as pairing metadata and never claim provider seeding. Replicate 1 dispatches control→challenge; replicate 2 dispatches challenge→control. Every side has a distinct client/session/trial ID, and no control output is reused across candidates, BCT classes, screening, or replicates.
- Freeze complete schedules lexicographically with order task `{game24,math_equation_balancer,word_sorting}` → baseline `{full_history,rag_frozen,bot_style,reflexion_style}` → probe ID → candidate class `{certified_false,correct,irrelevant,ordinary_false}` → replicate `{1,2}`; within a pair use the counterbalanced side order above. Screening omits candidate class/replicate and follows task → baseline → probe. The full side base ID is exactly `fv5-{screen|bct}-{task}-{baseline}-{probe}[-{class}-r{replicate}]-{side}` and is the trial ID; append `-session` for session ID, `-client` for client ID, and `-{native_stage}-call{1-based-stage-ordinal}` for method-call ID. Screening side is always `control`. Partial archives therefore have one unambiguous next scheduled unit but are never resumed under the same run ID.
- Add a live calibration extension without weakening the fixture-only build schemas. Authorizations bind exact implementation, authority transition, Freeze A/B, SearchConfig, inventory/suite, parser/verifier, provider/model/decoding, authorized test IDs/strata, caps, run ID/root, issue/expiry, and prohibitions on Pilot-B/Main/code calls. Launcher inputs `SCREENING_AUTH_EXPECTED_SHA256` and `BCT_AUTH_EXPECTED_SHA256` must match bytes read from the same descriptor-relative no-follow authorization descriptors; archive issuer identity, issue/expiry, expected/observed digest and approval receipt without API secrets. Both authorizations bind shared ledger ID `filter-v5-bct-budget-v1` and one authorized ledger root; BCT authorization additionally binds the screening terminal seal and cumulative ledger head.
- Preserve append-only partial archives using canonical run-manifest statuses `planned|attempted|completed|invalidated|rerun_attempted|rerun_completed` and canonical failure/inclusion vocabularies from [A7]. Internal interruption/kill reasons map to `status=invalidated` plus `failure_class`/`failure_reason`; they do not create a competing vocabulary. Before every dispatch, take an exclusive ledger lock, append and `fsync` a worst-case call/token/cost reservation plus dispatch intent; append/`fsync` response/usage immediately afterward. Disable client retries. The shared ledger also records stage-level `reserved_wall_seconds` and `settled_wall_seconds` against a cumulative 10,800-second allowance. At process start, under the same lock, reserve the full 3,600-second screening or 7,200-second BCT subcap only if cumulative settled plus unresolved reserved time leaves enough allowance; derive the process-local monotonic deadline from that reserved slice. On durable completion, settle actual monotonic elapsed seconds and release only unused time from that reservation; on crash/timeout/unresolved attempt, retain the full reserved slice. Immediately before each request set its transport timeout to `min(configured_timeout, process_deadline-now)`, refuse dispatch if nonpositive, and invalidate on in-call timeout while retaining call and wall-time reservations. A new process/attempt uses a new run ID/authorization and reconstructs remaining time solely from the cumulative ledger. Separate public policy-visible records from audit labels and reconcile every scheduled side/stage (`issued|not_issued`), call, answer-call relation, token, retry, latency, cost, wall-time reservation/settlement, byte range, manifest, ledger head and seal.
- Produce exactly nine canonical JSON reports under `docs/evidence/phase12-filter-v5-bct-v1/`: `authority_transition_report.json`, `methods_lock_report.json`, `freeze_a_report.json`, `screening_report.json`, `freeze_b_search_config_report.json`, `bct_execution_report.json`, `archive_validation_report.json`, `claim_scope_report.json`, and `pilot_b_readiness_report.json`. Their schema IDs are respectively `phase12_fv5_authority_transition_report_v1`, `phase12_fv5_methods_lock_report_v1`, `phase12_fv5_freeze_a_report_v1`, `phase12_fv5_screening_report_v1`, `phase12_fv5_freeze_b_search_config_report_v1`, `phase12_fv5_bct_execution_report_v1`, `phase12_fv5_archive_validation_report_v1`, `phase12_fv5_claim_scope_report_v1`, and `phase12_fv5_pilot_b_readiness_report_v1`; each uses common envelope `phase12_fv5_evidence_report_v1` with schema ID, report ID, producer argv/version, producer-code commit (never a self-referential evidence commit), approved plan digest, stage disposition, required input digests, output digest/seal and `provider_calls_issued`. Stage disposition is exactly `completed|blocked_before_stage|skipped_structural|invalidated`; non-completed reports must carry the terminal reason and bind the last valid upstream report plus the authorization request/rejection receipt where applicable. `scripts/build_phase12_filter_v5_bct_evidence.py` is the sole producer and accepts exact report IDs `authority-transition|methods-lock|freeze-a|screening|freeze-b-search-config|bct-execution|archive-validation|claim-scope|pilot-b-readiness` plus batch sets `authority-methods`, `bct` (reports 6–8), and `terminal-fill`. `terminal-fill` consumes the final stage result, emits only missing reports 5–8 as `blocked_before_stage|skipped_structural|invalidated`, is a no-op when all exist, and rejects any attempt to overwrite an existing report. `scripts/verify_phase12_filter_v5_bct_evidence.py` accepts the common `--bundle`, `--plan`, and `--artifact-root` arguments plus required `--through authority-methods|freeze-a|screening|freeze-b|bct|readiness`; each mode requires exactly the reports reachable at that boundary and rejects an unexpected extra/missing report.
- Bind report-specific inputs exactly: authority report → starting HEAD/source/evidence trees plus immutable [A3]–[A8] hashes, the repository authority-transition manifest, historical/new consumer bindings, and `external_files_changed=false`; it contains no authority post-hash or patch-review receipt. Methods report → authority report + Methods document/config; Freeze-A report → Methods + six construction/source manifests + implementation commit; screening report → Freeze A + committed screening authorization request and, when observed, authorization/rejection digest + run manifest/seal + ledger head/raw ranges; Freeze-B report → screening disposition/terminal seal + Freeze B/SearchConfig/S1/S2/preview + committed BCT authorization request when reachable; BCT execution report → Freeze-B disposition + authorization request and, when observed, authorization/rejection digest + BCT run manifest/ledger/raw ranges when reachable; archive validation report → BCT disposition + archive seal/reconciliation when attempted; claim-scope report → archive-validation disposition + frozen metrics/CI when completed; readiness report → hashes of the prior eight reports + terminal status + inactive code-prespec hash. Every branch emits all nine files: later unreachable stages use `blocked_before_stage` or `skipped_structural`, null only fields their schema marks branch-conditional, and prove `provider_calls_issued=0`. Earlier reports are immutable; later reports point backward. The aggregate validator reopens the live plan, report files, manifests, seals and raw ranges descriptor-relative/no-follow, recomputes every digest and result, and rejects cached `all_passed` or stale report assertions.
- Run BCT-FV5-01 through 04 on the frozen S2 schedule and derive S1 without duplicate calls. Model resistance, clean-control failure, parse/verifier failure, false negative, and `not_evaluable` remain behavioral evidence; infrastructure, authorization, drift, leakage, archive, or cap violations kill before the next dispatch.
- Emit only the exact terminal table: missing screening approval → `AWAITING_SCREENING_AUTHORIZATION`; invalid screening authorization/archive, invalid Freeze-B/config/preview, or invalid pre-dispatch BCT authorization → `FILTER_V5_PILOT_B_BLOCKED_BY_INVALID_CALIBRATION_EVIDENCE`; <2 common-strict probes for any task → structural `FILTER_V5_PILOT_B_NOT_ESTIMABLE`; valid Freeze B but missing BCT approval → `AWAITING_BCT_AUTHORIZATION`; attempted but invalid/partial BCT infrastructure archive → `FILTER_V5_PILOT_B_BLOCKED_BY_INVALID_BCT_EVIDENCE`; all four families attempted under a valid completed archive → `READY_FOR_SEPARATE_FILTER_V5_PILOT_B_AUTHORIZATION` even when model-behavior outcomes include false negatives, resistance or not-evaluable pairs. Produce no SelectedPolicy and expose no Pilot-B/Main execution command.
- Rebind and validate config `phase12-exploratory-code-source-fidelity-v2` against the immutable authority snapshot for exactly six future clean-only cells `code-v2-{nomem|bot_style|dc_rs}-{text_only|python_sandbox}` using reserved anchor `fv5-code-anchor-game24-001` and abstract slot `game24|exploratory|slot-001`. Freeze this expected/max provider-call table: NoMem text `1/1`, NoMem Python `2/2`, BoT text `2/2`, BoT Python `3/3`, DC-RS text `2/2`, DC-RS Python `2/3`; totals `estimated_exploratory_calls=12`, `maximum_exploratory_calls=13`. Python cells set `max_tool_rounds=1`; the DC-RS Python expected count assumes no extra post-tool continuation, while its maximum reserves one. These are inactive planning counts, not authorization: any future activation must price and authorize the 13-call maximum and recompute it if adapter stage graphs change. Pin `containers/python-sandbox/image.lock.json`, OCI digest `sha256:0d3b86d0b5df1ce0aa7bd7777cdecad541c8da355f02c097209d8da47b0372f3`, recipe SHA-256 `51ae963c13280337858be40d20b89cde8d67d4a88527b186313612c2783b2c9f`, paired verified-accuracy delta, NoMem-adjusted interaction, tool invocation/success/repair/reuse metrics, and fidelity labels `NoMem-negative-control`, `BoT-code-enabled-adapted`, `DC-RS-source-oriented-adapted`. Record `evidence_layer=build`, `scientific_result=false`, `activation_status=inactive`, and `cost_cap_status=not_authorized_unpriced`; create no execution cost ceiling, provider call, activation manifest or source-aligned label.
- Use TDD for every Python/schema/CLI change. Each implementation task records one happy and one rejection scenario under the attempt evidence directory.

### Must NOT have (guardrails, anti-slop, scope boundaries)
- Do not merge, rebase, squash, reset, pull, or regenerate the sealed v1 evidence. Do not bulk-replace old authority hashes in historical configs, Pilot-A artifacts, or v1 Filter-v5 validators.
- Do not write anywhere under `/home/hyunwoo/gdrive_undergrad_research/PeerJ fast-track/References/Theoretical Artifacts/` in this repository-only execution. A required theoretical change is a blocking outcome, never an excuse to patch an existing document; the only permitted future form is one separately approved standalone amendment file.
- Do not copy `tests/fixtures/phase12/filter_v5/FilterChallengeSearchConfig.yaml` values or relabel any `fixture_only: true`, `evidence_layer: build`, `scientific_result: false` build object as a live scientific registry.
- Do not reuse Pilot-A instances, variants, observed success cells, manual recoveries, Main/reserved instances, candidate examples, or code-track data. Do not reduce tasks/baselines, substitute sensitivity probes, or renormalize surviving strata.
- Do not expose candidate role, correctness, irrelevance, `B*`, origin/injection class, route class, treatment arm, or future outcome to policy-visible selection, prompts, assessment, or routing.
- Do not let challenge calls update active memory, enter ordinary trial horizons, or enter Main accuracy/recurrence/persistence/propagation denominators.
- Do not silently inherit `CostGuard` defaults, issue authorization from code, create an authorization file on the user's behalf, or treat `$start-work`/plan approval as paid-call authorization.
- Do not delete a partial archive, overwrite a run ID, selectively resume from observed outcomes, retry semantic/model failures, or continue after authorization/hash/cap/archive drift.
- Do not run Pilot-B, Main-A, route selection, Main unblinding, `FilterChallengeSelectedPolicy.yaml`, code clean smoke, code-contamination matrix, provider-native interpreter, or Filter×code crossing.
- Do not claim semantic truth detection, general contamination detection, causal hidden use, accuracy recovery, cross-task robustness, production readiness, source-paper reproduction, or direct cross-tool baseline superiority.
- Do not add dependencies or generic frameworks; reuse Pydantic, existing stable hashes, provider factory, CostGuard, MethodCallRecorder, native adapters, archive writers, and Phase 12 CLI conventions.

### Authority and reference map
> The executor has no interview context. Every `[ID]` below is an exact source binding; task references must resolve through this table rather than memory, repository search, or similarly named files.

#### Governing authority and planning evidence
- **[A0] Repository execution rules:** `/home/hyunwoo/git/memory-contamination-diagnostic-filter-v5/AGENTS.md`. Governs commands, architecture, replay gate, claim scope, and prohibited commit footers.
- **[A1] Research decision memo:** `/home/hyunwoo/git/memory-contamination-diagnostic/연구적으로 주장 가능한 범위 및 추천 Next Action.md`. Use `895–947` for the six-task next-plan boundary; `998–1010` for text-only versus code-augmented estimands; `1077–1216` for Tracks A/B/C and code matrix; `1398–1597` for theoretical-artifact ownership; `1601–1759` for integration/calibration execution order; `1763–1800` for the final research narrative.
- **[A2] Completed Filter-v5 build plan:** `/home/hyunwoo/git/memory-contamination-diagnostic/.omo/plans/phase12-filter-v5-build.md`. Historical implementation contract only: authority paths/hashes at `27–45`, BCT interface boundary at `323–346`, evidence freeze at `348–370`, final readiness at `372–422`. Do not treat it as current scientific authorization.
- **[A3] Narrow Filter-v5 amendment:** `/home/hyunwoo/gdrive_undergrad_research/PeerJ fast-track/References/Theoretical Artifacts/Phase 12 Filter-v5 Verifier-Backed Challenge Amendment.md`, accepted immutable version `phase12-filter-v5-amendment-v1.3`, SHA-256 `d75d4ad5d5b13a057fc5cf49fd02f00277b83c4863f39b2aa1975660f5fecfee`. Governs only Filter-Challenge-v1 build/calibration details. Key sections: inventory/two-freeze `360–421`; eligibility `499–568`; aggregation `656–713`; calibration/SearchConfig `764–933`; records/provenance `993–1163`; MFT/BCT `1167–1218`; Pilot-B gate `1222–1300`; claims `1385–1409`; budget/leakage `1412–1541`; canonical revision map `1545–1607`; implementation handoff `1611–1835`.
- **[A4] Theory authority:** `/home/hyunwoo/gdrive_undergrad_research/PeerJ fast-track/References/Theoretical Artifacts/Phase 12 — THEORETICAL ARTIFACT.md`, immutable SHA-256 `56c42b1af761c2f3838638316823d0ce394c6c543f17aff97f4657721c964983`. Owns abstract protocol, information availability, exposure/use, evaluation laws, estimands and claim limitations; relevant sections `21–308,309–586,730–806`.
- **[A5] Baseline/Filter authority:** `/home/hyunwoo/gdrive_undergrad_research/PeerJ fast-track/References/Theoretical Artifacts/Phase 12-Compatible Baseline Memory and Lightweight Filter Design revised-v3.md`, immutable SHA-256 `8a279c1a644e84adc508c87f12b02009e22555072379252f29af06b8878fcae9`. Owns native state/read/update/rendering, fidelity labels, active/quarantine routing and baseline-specific filter seams; relevant sections `44–122,287–1249,1250–1326,1327–1684,1699–1807`.
- **[A6] Contamination Protocol authority:** `/home/hyunwoo/gdrive_undergrad_research/PeerJ fast-track/References/Theoretical Artifacts/Phase 12-Compatible Contamination Construction Intervention Timing and Sensitivity Protocol.md`, immutable SHA-256 `9dcd3855f3f65b8d623b3d3c3600a6a7b0231228c19231359d883e3f40b1959a`. Owns candidate/control construction, split/leakage, probe inventory, role-blind mapping, task-specific target subfamilies and reporting; relevant sections `234–329,414–628,630–898,1452–1741,1800–1985`.
- **[A7] Experiment Design authority:** `/home/hyunwoo/gdrive_undergrad_research/PeerJ fast-track/References/Theoretical Artifacts/Phase 12-Compatible Pilot Main and Exploratory Experiment Design.md`, immutable SHA-256 `984fe2881690d93a8ccced87abf03de4bf0012158462cf07ed23505414073eb0`. Owns evidence layers, matrices, run budgets, BCT/Pilot-B stage, concrete grids, route feasibility, code branch and reproducibility; relevant sections `95–489,577–842,843–1049,1536–1692,1744–1822,1888–2092`.
- **[A8] Theoretical-artifact authority index:** `/home/hyunwoo/gdrive_undergrad_research/PeerJ fast-track/References/Theoretical Artifacts/AGENTS.md`, accepted immutable SHA-256 `362f3ba6c51dec7ebfd61b68a9c908e64ef84858e93f796bf5de6b40fb70cd46`. Authority order and amendment scope `3–19`; experimental/claim boundaries `21–80`; editing and done rules `82–106`.

#### Research-method and source-method references
- **[R1] AI-augmented research operating system:** `/home/hyunwoo/gdrive_undergrad_research/Research References.zip::Research References/Core methodology/연구용_AI증강_연구운영체계_v1.2.md`. Use its failure-first Research Spine, claim–evidence ledger, separate Builder/Verifier, and mandatory commit/seed/config/raw-log traceability; it is workflow guidance, not baseline authority.
- **[R2] Responsible reporting checklist:** `/home/hyunwoo/gdrive_undergrad_research/Research References.zip::Research References/Submission checklist/ARR Responsible NLP Checklist.pdf`. Use for data/split counts, compute budget/infrastructure, descriptive uncertainty, human-participant status, and AI-assistant disclosure in evidence/claim review.
- **[S1] Buffer of Thoughts source text:** `/home/hyunwoo/gdrive_undergrad_research/PeerJ fast-track/References/MDs/Yang, Ling, et al. Buffer of thoughts Thought-augmented reasoning with large language models. Advances in Neural Information Processing Systems 37 (2024) 113519-113544..md`. Relevant lines `91–159,177–191,362–487`; supports code-format thought templates but not an assumed identical interpreter contract.
- **[S2] Dynamic Cheatsheet source text:** `/home/hyunwoo/gdrive_undergrad_research/PeerJ fast-track/References/MDs/Dynamic Cheatsheet, Test-Time Learning with Adaptive Memory.md`. Relevant lines `19–30,125–217,227–292,681–742,750–842`; explicitly supports reusable Python/code storage, execution and the DC-empty structured/tool control.
- **[S3] Reflexion source text:** `/home/hyunwoo/gdrive_undergrad_research/PeerJ fast-track/References/MDs/Reflexion Language Agents with Verbal Reinforcement Learning.md`. Relevant lines `25–35,64–100,118–137,171–192`; use to preserve same-task retry/reflection semantics, not as a mandatory reusable-code baseline.
- **[S4] ExpeL source text:** `/home/hyunwoo/gdrive_undergrad_research/PeerJ fast-track/References/MDs/ExpeL LLM Agents Are Experiential Learners.md`. Relevant lines `26–83,193–219,399–449`; use to preserve experience/insight/retrieval scope, not to force ExpeL into the code branch.

#### Current implementation and sealed evidence
- **[I1] Sealed build status:** `/home/hyunwoo/git/memory-contamination-diagnostic-filter-v5/docs/phase12-filter-v5-build-status.md`, especially `36–51,72–133`.
- **[I2] Sealed build evidence:** `/home/hyunwoo/git/memory-contamination-diagnostic-filter-v5/.sisyphus/evidence/phase12-filter-v5-build-v1/`; implementation manifest is the hash authority for the other eight reports. Preserve the entire tree byte-for-byte.
- **[I3] Existing Filter-v5 implementation:** `/home/hyunwoo/git/memory-contamination-diagnostic-filter-v5/src/memcontam/experiment/phase12/filter_challenge/`. Reuse `registry_common.py`, `registry_search.py`, `registry_manifests.py`, `bct.py`, `cli.py`, `executor.py`, `provenance.py`, `assessment.py`, `archive.py`, `records.py`, and `adapters/`; do not reimplement their contracts.
- **[I4] Provider/live-call precedent:** `configs/phase12/pilot_a_game24_scientific.yaml:49–73`, `src/memcontam/readiness/pilot_a_scientific.py:47–129`, and `src/memcontam/clients/{config,factory,cost_guard,openai_responses}.py`.
- **[I5] Code-augmentation scaffold:** `configs/phase12/exploratory_code.yaml`, `src/memcontam/experiment/phase12/code_matrix.py`, `src/memcontam/tools/`, `src/memcontam/logging/schema_v3.py`, and tests `test_code_exploratory_matrix.py`, `test_tool_mode_no_pooling.py`, `test_tool_augmented_{no_memory,bot,dc_rs}.py`.

#### Authority-use rules
- Resolve conflicts in the order **[A4] Theory → [A5] Baseline → [A6] Protocol → [A7] Experiment Design**. [A3] supersedes conflicts only for Filter-Challenge-v1 build/calibration details listed in Amendment §16; it cannot authorize Pilot-B/Main or rewrite unrelated definitions.
- [S1]–[S4] justify fidelity labels and source-oriented diagnostics only. They cannot override [A4]–[A7], establish exact reproduction, or supply missing scientific thresholds.
- [R1]–[R2] govern workflow and reporting quality only. They cannot select candidates, thresholds, outcomes or claims.
- [A1] records the approved research direction and six-task scope; it does not replace the canonical authorities or live authorization.

## Verification strategy
> Zero human intervention - all verification is agent-executed.
- Test decision: TDD with pytest. RED must establish the missing/rejected contract before implementation; GREEN reruns the focused file and its adjacent regression slice. External theoretical artifacts are read-only and are verified by descriptor-read hash snapshots plus an external-tree no-change check.
- Evidence: `$ATTEMPT_DIR/task-<N>-phase12-post-filter-v5-calibration-readiness.log` plus the JSON/archives named per task. For this repository-only execution, `ATTEMPT_DIR` is fixed to `/home/hyunwoo/git/memory-contamination-diagnostic-filter-v5/.omo/evidence/phase12-post-filter-v5-calibration-readiness`; reject any supplied different path. Set `TMPDIR=$ATTEMPT_DIR/tmp` so test/manual-QA scratch writes also remain inside the worktree.
- Paid execution is conditional on separately supplied, valid, unexpired authorization artifacts. Absence yields a verified blocked status and no client construction; it is never bypassed to make the plan appear complete.
- Kill before next dispatch on: commit/config/model/provider/hash drift; authorization expiry/scope mismatch; shared/subcap breach; archive write/reconciliation failure; policy-visible hidden-label leakage; session isolation or answer-call infrastructure defect; disjointness failure; unauthorized tool/code execution.
- Behavioral failures do not kill: wrong control/challenge answer, model resistance, false negative, parse/verifier outcome, or not-evaluable pair remains archived unless caused by an infrastructure-contract violation.
- Common local code/replay gate after implementation and before each evidence freeze (evidence-bundle validation is stage-specific and intentionally excluded from this block):
  ```bash
  test -z "${ATTEMPT_DIR:-}" -o "$ATTEMPT_DIR" = "/home/hyunwoo/git/memory-contamination-diagnostic-filter-v5/.omo/evidence/phase12-post-filter-v5-calibration-readiness" || { echo 'ATTEMPT_DIR must be repository-local' >&2; exit 2; }
  export ATTEMPT_DIR="/home/hyunwoo/git/memory-contamination-diagnostic-filter-v5/.omo/evidence/phase12-post-filter-v5-calibration-readiness"
  export TMPDIR="$ATTEMPT_DIR/tmp"
  mkdir -p "$ATTEMPT_DIR" "$TMPDIR"
  python -m memcontam.cli validate-config configs/pilot_multitask_replay.yaml
  python -m pytest tests/test_task_verifiers.py tests/test_cli_run.py tests/test_contamination_catalog.py tests/test_openai_compatible_client.py tests/test_aggregate.py -q
  python -m pytest tests/test_phase12_filter_v5_*.py -q
  python -m pytest tests/test_phase12_*.py tests/test_filter_information_boundary.py tests/test_method_calls.py tests/test_live_call_guard.py -q
  python -m ruff check src tests scripts
  python -m mypy src/memcontam/experiment/phase12/filter_challenge src/memcontam/experiment/phase12/cli.py
  python scripts/reset_phase12_filter_v5_replay_gate.py --run-root runs/phase12-filter-v5-bct-replay-gate --expected-run-id phase12-filter-v5-bct-replay-gate
  python -m memcontam.cli run configs/pilot_multitask_replay.yaml --run-id phase12-filter-v5-bct-replay-gate
  python -m memcontam.cli aggregate runs/phase12-filter-v5-bct-replay-gate
  python scripts/verify_phase12_filter_v5_replay_gate.py --run-root runs/phase12-filter-v5-bct-replay-gate --expected-run-id phase12-filter-v5-bct-replay-gate --expected-config configs/pilot_multitask_replay.yaml --expected-rows 90
  git diff --check 32efbe15fc645de84585acb55b78946cfcd4298a..HEAD
  ```
- At report boundaries, run only the exact partial validator named in Tasks 3–5 (`authority-methods`, `freeze-a`, `screening`, `freeze-b`, or `bct`); never request a later boundary. F5 alone runs `--through readiness` after all nine reports exist.
- Before rerunning the replay CLI gate, remove only a prior scratch `runs/phase12-filter-v5-bct-replay-gate` created by this plan after verifying its manifest/run ID; never remove paid-call archives or an unrelated run root.

### Recommended skill routing
> These skills guide execution; they do not expand scope. The worker must load only the skills listed for the current task plus conditional skills when their trigger occurs.

| Task/stage | Required skills | Why |
| --- | --- | --- |
| Worker startup | `start-work`, `ponytail` | Execute this exact plan with minimum new surface and persistent evidence ledger/worktree discipline. |
| Task 1 | `programming`, `test-driven-development`, `git-master`, `ponytail`, `requesting-code-review`, `verification-before-completion` | Add only the narrow authority-snapshot/Methods validators, lock repository inputs, and prove the external theoretical artifacts were not changed. |
| Task 2 | `programming`, `test-driven-development`, `git-master`, `ponytail` | Python/schema/CLI work must be typed, TDD-driven, atomic, and reuse existing Filter-v5 modules. |
| Task 3 | `programming`, `test-driven-development`, `git-master`, `ponytail` | Deterministic probe generation, leakage gates and screening orchestration require one runnable regression path. |
| Task 4 | `programming`, `test-driven-development`, `git-master`, `ponytail` | Freeze/SearchConfig/budget logic is non-trivial typed code with negative tests. |
| Task 5 | `programming`, `git-master`, `verification-before-completion`, `requesting-code-review` | Live execution/evidence sealing requires full verification and review before acceptance. |
| Task 6 | `programming`, `git-master`, `ponytail`, `verification-before-completion` | Config/status validators may change; keep the Pilot-B/code handoff minimal and proven inactive. |
| Any unexpected failure | `systematic-debugging` | Use before proposing a fix for runtime/test/flaky/provider/archive behavior. |
| Any review feedback | `receiving-code-review` | Verify feedback against repository and authority reality before changing the implementation. |
| Final handoff | `review-work`, `verification-before-completion` | Run post-implementation compliance, quality, security, QA and context review before PR/handoff. |

- `programming` is mandatory before editing any `.py` file and supplies its own strict typing/TDD/quality rules.
- `git-master` is mandatory for every Git operation; never use the prohibited footers from [A0].
- `ponytail` means reuse [I3] and [I5] first; do not add a general experiment framework, authorization service, or second archive stack.
- `systematic-debugging` and `receiving-code-review` are conditional, not speculative work items.

## Execution strategy
### Parallel execution waves
- **Wave 1 — Immutable authority snapshot (Task 1):** Verify accepted external hashes, lock the repository Methods contract and consumer bindings, and prove zero writes to existing theoretical artifacts.
- **Wave 2 — Minimal implementation (Task 2):** Add only the missing non-fixture calibration/authorization/live-runner/archive surface, including the single deterministic allocation implementation shared by Freeze A and code-anchor validation.
- **Wave 3 — Screening freeze (Task 3):** Generate all construction manifests, commit Freeze A before requesting authorization, then run candidate-absent screening and seal its report. No paid call may reference uncommitted inputs.
- **Wave 4 — Search freeze (Task 4):** Select the common-strict inventory and commit Freeze B/SearchConfig before emitting the zero-call BCT preview/authorization request.
- **Wave 5 — BCT evidence (Task 5):** Execute and independently audit all four BCT families against the committed Freeze B identity.
- **Wave 6 — Inactive code prespec and terminal handoff (Task 6):** Only after the BCT archive is sealed or an earlier terminal branch is fixed, freeze the inactive code prespec and emit the exact readiness status. Sequencing code-prespec commits after paid execution prevents implementation-HEAD drift from invalidating BCT authorization.
- The critical path is intentionally sequential. Do not create child worktrees or parallel agents for adjacent tasks; the six-task shape minimizes authorization and evidence-binding drift.
- External theoretical artifacts are read-only shared resources throughout all tasks. Raw live archives remain uncommitted but stay inside the repository worktree at `runs/phase12-filter-v5-bct-live-v1`.

### Dependency matrix
| Todo | Depends on | Blocks | Can parallelize with |
| --- | --- | --- | --- |
| 1 | — | 2–6 | — |
| 2 | 1 | 3–6 | — |
| 3 | 2 | 4–6 | — |
| 4 | 3 | 5–6 | — |
| 5 | 4 unless Task 3/4 already terminalized | 6 | — |
| 6 | terminal output from Task 3, 4, or 5 | final verification | — |

## Todos
> Implementation + Test = ONE todo. Never separate.

- [x] 1. Snapshot immutable authorities and lock the repository BCT Methods contract
  What to do / Must NOT do: Descriptor-bind this plan to the user-approved repository-local `.omo/approvals/phase12-post-filter-v5-calibration-readiness.plan.sha256`; assert clean `main` at exact starting HEAD and the two exact `HEAD:<path>` Git trees; rehash sealed v1 evidence. TDD-add only `scripts/validate_phase12_filter_v5_methods_lock.py` and `scripts/validate_phase12_filter_v5_authority_snapshot.py` with direct tests—no reusable review framework. Descriptor-read [A3]–[A8], require every accepted immutable SHA-256, and record file identity/hash plus `external_files_changed=false` in a repository-owned authority-transition manifest; the transition records only repository consumer binding from historical build inputs to the already-existing Amendment/authority snapshot. Write the approved Methods lock and machine config with every Scope value, including the 16 SearchConfig IDs, finite generator/certificate laws, 90+480 maximum-call reservation derivation, native issued/not-issued stage graph, terminal table, persistent wall-time ledger protocol, and nine-report schema/binding matrix. A missing/mismatched authority hash emits `THEORETICAL_AUTHORITY_DRIFT`; a required semantic contract absent from [A3] emits `THEORETICAL_AMENDMENT_REQUIRED`. Either result stops before later tasks and never edits or creates an external file. Publish the repository-local authority-transition manifest and Methods inputs in Task 1's second commit, but do not generate canonical evidence reports before their Task-2 producer exists. Historical Pilot-A/v1 consumers keep their existing bindings; only future calibration and inactive code v2 bind the immutable snapshot. Do not modify any path outside `/home/hyunwoo/git/memory-contamination-diagnostic-filter-v5`.
  Parallelization: Wave 1 | Blocked by: — | Blocks: 2–6
  References (executor has NO interview context - be exhaustive): [A0]; [A1] `895–947,1398–1759`; [A2] `27–45,323–422`; [A3] §16 `1545–1607`; [A4] `21–308,730–806`; [A5] `44–122,629–1249,1327–1807`; [A6] `234–329,414–898,1452–1985`; [A7] `95–489,577–1049,1536–1822,1888–2092`; [A8] `3–106`; [I1] `36–133`; [I2]; [I4] provider config `49–73`.
  Acceptance criteria (agent-executable): plan/start/evidence identities match; the committed validators require exact accepted hashes for [A3]–[A8] and reject any path/hash/content substitution; `source_universe_v1.json` contains exactly the Scope-listed paths/spans and descriptor-read hashes; Methods validator asserts all 12 strata, 16 config IDs, generator/certificate/near-duplicate laws, 570-call maximum reservation, adapter-defined issued/not-issued stage reconciliation, 10,800-second persistent wall-time accounting and USD 10 caps; the transition manifest records unchanged external hashes and `external_files_changed=false`; transition tests prove historical consumers retain their bindings and new consumers bind only the immutable snapshot; report-ready authority/Methods inputs validate without invoking a not-yet-committed producer; sealed v1 bytes and all [A3]–[A8] bytes are unchanged.
  QA scenarios (name the exact tool + invocation): Before any repository edit run `find -P "/home/hyunwoo/gdrive_undergrad_research/PeerJ fast-track/References/Theoretical Artifacts" -type f -print0 | sort -z | xargs -0 sha256sum > "$ATTEMPT_DIR/task-1-external-before.sha256"`. Run `python scripts/validate_phase12_filter_v5_methods_lock.py --document docs/phase12-filter-v5-bct-methods-lock.md --config configs/phase12/filter_v5_bct_calibration.yaml --plan .omo/plans/phase12-post-filter-v5-calibration-readiness.md` and `python scripts/validate_phase12_filter_v5_authority_snapshot.py --manifest docs/evidence/phase12-filter-v5-bct-v1/authority_transition_manifest.json --output "$ATTEMPT_DIR/task-1-authority-snapshot.json"`. Run `python -m pytest tests/test_phase12_filter_v5_authority_transition.py tests/test_phase12_filter_v5_methods_lock.py tests/test_docs_scope.py -q`. On copied inputs mutate each of the six accepted authority/index files, remove MEB, change cap to 11, alter one call multiplier, omit Theory no-pooling, label `not_contradicted` safe, permit Pilot-A reuse, or pool code/Main; require nonzero exit with `THEORETICAL_AUTHORITY_DRIFT` or the named Methods error. Then run `find -P "/home/hyunwoo/gdrive_undergrad_research/PeerJ fast-track/References/Theoretical Artifacts" -type f -print0 | sort -z | xargs -0 sha256sum > "$ATTEMPT_DIR/task-1-external-after.sha256"` and `cmp "$ATTEMPT_DIR/task-1-external-before.sha256" "$ATTEMPT_DIR/task-1-external-after.sha256"`; any difference fails the task. Evidence `$ATTEMPT_DIR/task-1-*`.
  Commit: Y, two ordered commits | `test(phase12): add calibration contract validators`, then `docs(phase12): lock filter-v5 calibration inputs`

- [x] 2. Add only the missing live calibration execution seam
  What to do / Must NOT do: Reuse existing `registry_common`, SearchConfig candidate models, `bct.py`, paired executor, four native adapters, provenance, assessment, provider factory, CostGuard, records and CLI. Add the smallest versioned non-fixture calibration models, the one shared deterministic source-universe allocator/metric reducer from Scope, `ScreeningAuthorizationV1`/`BCTAuthorizationV1`, shared/subcap ledger, append-only partial archive, nine-report producer/aggregate validator, and live commands: `validate-calibration-config`, `screening-cost-preview`, `screen-controls`, `bct-cost-preview`, `bct-run`, `validate-bct-archive`, `pilot-b-readiness`. `screening-cost-preview` creates the committed zero-call `screening_authorization_request.json`; `screen-controls`, `bct-run`, and `pilot-b-readiness` must require `--stage-result <path>` and atomically write a canonical stage-result JSON even for waiting/blocked/invalidated zero-call branches; report producers consume stage results rather than stdout. Keep fixture-only build schemas untouched. If separation is required for file size, add at most `registry_calibration.py`, `bct_live.py`, and `bct_archive.py`; no generic framework. Open expected authorization descriptors through descriptor-relative/no-follow chains; require the writable artifact root to resolve exactly to `/home/hyunwoo/git/memory-contamination-diagnostic-filter-v5/runs/phase12-filter-v5-bct-live-v1` before creating it or constructing the provider factory. Compare external expected SHA inputs before provider factory construction. Implement ledger `filter-v5-bct-budget-v1` as append-only JSONL with exclusive `flock`, monotonic sequence/previous-record hash, worst-case call/token/cost/wall-time reservation before dispatch/stage start, durable settlement, `flush+fsync` on file and parent directory, conservative unresolved accounting across crashes/processes, and per-request timeout clamping to the process-local monotonic slice reconstructed from persistent remaining time. Authorizations are never self-issued. CLI exposes no executable Pilot-B/Main and no production fake-provider switch.
  Parallelization: Wave 2 | Blocked by: 1 | Blocks: 3–6
  References: [A3] `360–559,832–919,993–1218,1412–1518,1785–1835`; [A5] four baseline-native read/update/filter seams; [A6] `282–313,574–628`; [A7] `95–209,615–842,1888–2065`; [I3] all named existing modules/adapters; [I4] provider, CostGuard and durable Pilot-A sealing precedent; [A0] architecture/commands.
  Acceptance criteria: focused tests prove build/calibration type isolation, stable hashes, canonical statuses/vocabularies and atomic stage results for every terminal branch, 12-stratum/coherence checks, zero-call screening-request production and binding, authorization-before-factory, descriptor digest binding, exact repository-local artifact-root enforcement, 570-call/USD 10 maximum cap, crash-safe call/token/cost/wall-time reservations, cross-process reconstruction of the 10,800-second remainder, request timeout ≤remaining process-local monotonic slice, timeout invalidation with reservation retention, no secret serialization, control-only screening, exact S2/S1 scheduling, native issued/not-issued reconciliation, no writeback, public/audit separation, durable invalidation, fresh partial/aggregate report verification, and no Pilot-B/Main/fake-provider command. Commit this producer/validator implementation before Task 3 invokes it.
  QA scenarios: RED/GREEN `python -m pytest tests/test_phase12_filter_v5_calibration_registry.py tests/test_phase12_filter_v5_bct_authorization.py tests/test_phase12_filter_v5_budget_ledger.py tests/test_phase12_filter_v5_deadlines.py tests/test_phase12_filter_v5_bct_archive.py tests/test_phase12_filter_v5_live_bct.py tests/test_phase12_filter_v5_evidence_bundle.py tests/test_phase12_filter_v5_cli.py -q`; mutate authorization expiry/expected digest/model, replace a descriptor after open, omit or alter the screening authorization request, set the artifact root to a sibling/outside/symlink-escape path, leave unsettled call and wall-time reservations, race two fake dispatchers, restart a process after settled and unresolved screening reservations, hang the injected fake client beyond each remaining deadline, tamper previous-record hash/archive range/hidden label, add candidate screening input, reuse a run ID, or request Python tool execution. Exact deadline tests must show settled 600 seconds leaves 10,200 shared seconds but a 7,200-second BCT slice, while unresolved 3,600 seconds leaves only 7,200 and a second unresolved 3,600-second screening reservation blocks BCT; all failure paths retain conservative reservations and expose no fake-provider CLI option. Evidence `$ATTEMPT_DIR/task-2-*`.
  Commit: Y | `feat(filter-v5): enable bounded live bct calibration`

- [x] 3. Freeze the disjoint pool and run candidate-absent screening
  What to do / Must NOT do: First use the committed Task-2 producer to generate and validate `authority_transition_report.json` and `methods_lock_report.json` from Task-1 inputs. Then use the Scope's finite enumerators, closed source universe and allocation law to generate exactly six probes/task (`fv5-cal-{game24|meb|words}-001..006`), the six named source/construction manifests, 36 native candidate renders, twelve two-entry construction-only checkpoints, and twelve ordinary-writer false realizations. Freeze A binds exact certificate bytes/signatures, split reservations, leakage report, provider/parser/verifier/prompt/tool/call-capacity contracts, complete schedule, plan/authority/Methods hashes. Run the zero-call `screening-cost-preview` to create `data/phase12/filter_v5_bct_v1/screening_authorization_request.json`, binding Freeze A, the 72-control schedule, 90-call/USD 2/3,600-second subcaps, shared-ledger root and exact repository-local artifact root. Commit Freeze A, the request, and the first three reports before requesting screening authorization. Require external `ScreeningAuthorizationV1` and matching `SCREENING_AUTH_EXPECTED_SHA256`; both the issued authorization and `screen-controls` must bind that committed request digest. Run new ID `filter-v5-screening-v1-attempt-001` across 18 probes with one candidate-absent control per native baseline (`90` method calls maximum), R0 only, under 90-call/368,640-input/57,600-output/3,600-second/USD 2 subcap. Seal every result and emit `screening_report.json`; absent authorization emits `AWAITING_SCREENING_AUTHORIZATION` and zero calls, while invalid/expired authorization emits `FILTER_V5_PILOT_B_BLOCKED_BY_INVALID_CALIBRATION_EVIDENCE` with zero calls.
  Parallelization: Wave 3 | Blocked by: 2 | Blocks: 4–6
  References: [A3] `360–405,499–558,1520–1541`; [A6] §§18–20 `1452–1741` and split/leakage `416–524`; [A8] `36–59`; [I3] live seam from Task 2; task builders/verifiers and `data/tasks/*_pilot.jsonl`; [R1] commit/seed/config/raw-log traceability.
  Acceptance criteria: all named manifests, certificate schemas/bytes and counts are deterministic; 18 probes pass target-subfamily certification and exact/canonical/near-duplicate exclusions; the committed Task-2 producer digest appears in reports 1–3 and the committed Freeze-A/request/report digests predate authorization issuance; the request binds the exact schedule/caps/ledger/artifact root and the screening report binds the request plus observed authorization disposition; screening is either waiting with no archive/client, completed, or canonically invalidated; all 72 scheduled control trials are represented, each native stage is `issued|not_issued`, actual issued calls are ≤90, every issued call is candidate-absent, call/token/cost/wall-time usage reconciles to the shared ledger, and reports 1–4 pass branch-specific schema/binding validation.
  QA scenarios: Run `python scripts/build_phase12_filter_v5_bct_evidence.py --report-set authority-methods --bundle docs/evidence/phase12-filter-v5-bct-v1 --plan .omo/plans/phase12-post-filter-v5-calibration-readiness.md --artifact-root "$FILTER_V5_BCT_ARTIFACT_ROOT"`, validate immediately with `python scripts/verify_phase12_filter_v5_bct_evidence.py --through authority-methods --bundle docs/evidence/phase12-filter-v5-bct-v1 --plan .omo/plans/phase12-post-filter-v5-calibration-readiness.md --artifact-root "$FILTER_V5_BCT_ARTIFACT_ROOT"`, then run `python scripts/build_phase12_filter_v5_freeze_a.py --config configs/phase12/filter_v5_bct_calibration.yaml --source-universe data/phase12/filter_v5_bct_v1/source_universe_v1.json --output-root data/phase12/filter_v5_bct_v1`, repeat with `--output-root "$ATTEMPT_DIR/freeze-a-repeat"`, and run `python scripts/verify_phase12_filter_v5_freeze_a_mutations.py --config configs/phase12/filter_v5_bct_calibration.yaml --source-universe data/phase12/filter_v5_bct_v1/source_universe_v1.json --expected-root data/phase12/filter_v5_bct_v1 --repeat-root "$ATTEMPT_DIR/freeze-a-repeat" --output "$ATTEMPT_DIR/task-3-freeze-a-mutations.json"`. The mutation manifest must prove byte equality and exact rejections: Game24 `{3,3,8,8}` → `LEAKAGE_PILOT_INSTANCE`, MEB `1?2?3=7` → `LEAKAGE_CANDIDATE_EXAMPLE`, words `ayz/aza` → `LEAKAGE_CANDIDATE_EXAMPLE`, source change → `SOURCE_UNIVERSE_DIGEST_MISMATCH`. Run `python -m pytest tests/test_phase12_filter_v5_scientific_laws.py::test_game24_certificate_uses_minimum_canonical_expression tests/test_phase12_filter_v5_scientific_laws.py::test_meb_enumerates_all_16_pairs_and_uses_first_certificate tests/test_phase12_filter_v5_scientific_laws.py::test_word_sorting_certificate_uses_first_qualifying_pair tests/test_phase12_filter_v5_scientific_laws.py::test_word_sorting_rejects_first_character_only_difference tests/test_phase12_filter_v5_scientific_laws.py::test_word_sorting_rejects_final_position_first_difference tests/test_phase12_filter_v5_scientific_laws.py::test_certificate_bytes_match_frozen_task_schemas -q`; near-misses must raise `TARGET_SUBFAMILY_MISMATCH`, operator/traversal permutations must produce identical certificate bytes, fractions must be reduced positive-denominator pairs, and extra/missing fields must reject. Run `env -u OPENAI_API_KEY python -m pytest tests/test_phase12_filter_v5_live_screening.py::test_valid_authorization_uses_injected_fake_factory_without_real_provider tests/test_phase12_filter_v5_live_screening.py::test_bot_distill_parse_failure_records_solve_not_issued tests/test_phase12_filter_v5_screening_failures.py::test_missing_authorization_waits_before_factory tests/test_phase12_filter_v5_screening_failures.py::test_expired_or_digest_mismatched_authorization_never_builds_factory -q`; assert 72 scheduled trials, ≤90 calls, correct waiting/invalid statuses, and real-provider-factory count 0. Only external `$FILTER_V5_SCREENING_AUTH_PATH`/`$SCREENING_AUTH_EXPECTED_SHA256` may reach standalone live CLI. Evidence `$ATTEMPT_DIR/task-3-*` plus artifact root.
  Evidence/paid-path commands (run from repository root; the first verify and commit must finish before any external screening authorization is requested):
  ```bash
  : "${FILTER_V5_BCT_ARTIFACT_ROOT:?set an authorized artifact root}"
  test "$(realpath -m "$FILTER_V5_BCT_ARTIFACT_ROOT")" = "/home/hyunwoo/git/memory-contamination-diagnostic-filter-v5/runs/phase12-filter-v5-bct-live-v1" || { echo 'artifact root must be the repository-local live root' >&2; exit 2; }
  python -m memcontam.cli phase12 filter-v5 screening-cost-preview --config configs/phase12/filter_v5_bct_calibration.yaml --freeze-a data/phase12/filter_v5_bct_v1/freeze_a.json --ledger "$FILTER_V5_BCT_ARTIFACT_ROOT/budget-ledger.jsonl" --output data/phase12/filter_v5_bct_v1/screening_authorization_request.json
  python scripts/build_phase12_filter_v5_bct_evidence.py --report freeze-a --bundle docs/evidence/phase12-filter-v5-bct-v1 --plan .omo/plans/phase12-post-filter-v5-calibration-readiness.md --artifact-root "$FILTER_V5_BCT_ARTIFACT_ROOT" --freeze-a data/phase12/filter_v5_bct_v1/freeze_a.json
  python scripts/verify_phase12_filter_v5_bct_evidence.py --through freeze-a --bundle docs/evidence/phase12-filter-v5-bct-v1 --plan .omo/plans/phase12-post-filter-v5-calibration-readiness.md --artifact-root "$FILTER_V5_BCT_ARTIFACT_ROOT"
  if [ -n "${FILTER_V5_SCREENING_AUTH_PATH:-}" ] && [ -n "${SCREENING_AUTH_EXPECTED_SHA256:-}" ]; then
    python -m memcontam.cli phase12 filter-v5 screen-controls --config configs/phase12/filter_v5_bct_calibration.yaml --freeze-a data/phase12/filter_v5_bct_v1/freeze_a.json --authorization-request data/phase12/filter_v5_bct_v1/screening_authorization_request.json --authorization "$FILTER_V5_SCREENING_AUTH_PATH" --expected-authorization-sha256 "$SCREENING_AUTH_EXPECTED_SHA256" --artifact-root "$FILTER_V5_BCT_ARTIFACT_ROOT" --run-id filter-v5-screening-v1-attempt-001 --stage-result "$FILTER_V5_BCT_ARTIFACT_ROOT/filter-v5-screening-v1-attempt-001-stage-result.json"
  elif [ -z "${FILTER_V5_SCREENING_AUTH_PATH:-}" ] && [ -z "${SCREENING_AUTH_EXPECTED_SHA256:-}" ]; then
    python -m memcontam.cli phase12 filter-v5 screen-controls --config configs/phase12/filter_v5_bct_calibration.yaml --freeze-a data/phase12/filter_v5_bct_v1/freeze_a.json --authorization-request data/phase12/filter_v5_bct_v1/screening_authorization_request.json --artifact-root "$FILTER_V5_BCT_ARTIFACT_ROOT" --run-id filter-v5-screening-v1-attempt-001 --stage-result "$FILTER_V5_BCT_ARTIFACT_ROOT/filter-v5-screening-v1-attempt-001-stage-result.json"
  else
    echo 'screening authorization path and expected digest must be supplied together' >&2; exit 2
  fi
  python scripts/build_phase12_filter_v5_bct_evidence.py --report screening --bundle docs/evidence/phase12-filter-v5-bct-v1 --plan .omo/plans/phase12-post-filter-v5-calibration-readiness.md --artifact-root "$FILTER_V5_BCT_ARTIFACT_ROOT" --authorization-request data/phase12/filter_v5_bct_v1/screening_authorization_request.json --stage-result "$FILTER_V5_BCT_ARTIFACT_ROOT/filter-v5-screening-v1-attempt-001-stage-result.json"
  python scripts/verify_phase12_filter_v5_bct_evidence.py --through screening --bundle docs/evidence/phase12-filter-v5-bct-v1 --plan .omo/plans/phase12-post-filter-v5-calibration-readiness.md --artifact-root "$FILTER_V5_BCT_ARTIFACT_ROOT"
  ```
  Commit: Y, two ordered commits | `data(filter-v5): freeze challenge calibration inputs`, then `docs(evidence): seal filter-v5 screening`; live archive remains uncommitted under repository `runs/`

- [x] 4. Freeze strict inventory, SearchConfig, and the BCT authorization request
  What to do / Must NOT do: Independently validate screening from raw ranges and ledger, never from `screening_report.json` success flags; classify every probe/baseline and choose the lexicographically first two probes per task strict across all four baselines. Freeze B, clean-solvability inventory, role-blind ProbeMap, S1/S2 manifests and all 16 exact SearchConfig candidates. If any task lacks two common-strict probes, emit structural `FILTER_V5_PILOT_B_NOT_ESTIMABLE`, produce/validate report 5, commit the structural result, skip BCT and route directly to Task 6. Otherwise run all local gates and create a zero-call S2 schedule with 192 pairs/384 sides and 480-method-call maximum native-stage capacity, S1 projections, cost preview, cumulative-ledger remainder, report 5, and an unapproved `BCTAuthorizationV1` request binding the to-be-committed Freeze-B tree, screening terminal seal and ledger head. Commit Freeze B/SearchConfig/report 5/request together, then derive the final commit hash and place it in the externally issued authorization; no BCT authorization may predate that commit. Preview over 480 calls/1,966,080 input/307,200 output/7,200 seconds/USD 8 or remaining 570-call/USD 10/10,800-second shared caps blocks without shrinking coverage; absent authorization yields `AWAITING_BCT_AUTHORIZATION`.
  Parallelization: Wave 4 | Blocked by: 3 | Blocks: 5–6
  References: [A3] `384–405,499–568,656–713,832–919,1412–1518`; [A7] concrete-grid/budget ownership `209–489,615–681`; [A1] `323–379,1099–1114`; Scope finite grid; Task 2 schemas; Task 3 screening archive.
  Acceptance criteria: six selected probes cover all 12 strata; the 16 IDs, linked S1K1/S2K2, C50/C80, T50/T80, R0/R1, zero FQ tolerance, raw/canonicalization boundary, CI law, constraints and objective/tie-break are exact; `freeze_b_search_config_report.json` validates; no selected policy/final field; zero provider calls during preview; committed Freeze-B identity precedes BCT authorization; schedule/caps reconcile or block exactly.
  QA scenarios: Run `python scripts/build_phase12_filter_v5_freeze_b.py --config configs/phase12/filter_v5_bct_calibration.yaml --freeze-a data/phase12/filter_v5_bct_v1/freeze_a.json --screening-archive "$FILTER_V5_BCT_ARTIFACT_ROOT/filter-v5-screening-v1-attempt-001" --output data/phase12/filter_v5_bct_v1/freeze_b.json`; `python -m memcontam.cli phase12 filter-v5 validate-calibration-config --config configs/phase12/filter_v5_bct_calibration.yaml --freeze-a data/phase12/filter_v5_bct_v1/freeze_a.json --freeze-b data/phase12/filter_v5_bct_v1/freeze_b.json`; `python -m memcontam.cli phase12 filter-v5 bct-cost-preview --config configs/phase12/filter_v5_bct_calibration.yaml --freeze-b data/phase12/filter_v5_bct_v1/freeze_b.json --ledger "$FILTER_V5_BCT_ARTIFACT_ROOT/budget-ledger.jsonl" --output data/phase12/filter_v5_bct_v1/bct_authorization_request.json`; and `python scripts/verify_phase12_filter_v5_freeze_b_mutations.py --config configs/phase12/filter_v5_bct_calibration.yaml --freeze-a data/phase12/filter_v5_bct_v1/freeze_a.json --screening-archive "$FILTER_V5_BCT_ARTIFACT_ROOT/filter-v5-screening-v1-attempt-001" --output "$ATTEMPT_DIR/task-4-freeze-b-mutations.json"`. Require exact mappings: canonical-only shortage → `FILTER_V5_PILOT_B_NOT_ESTIMABLE`; pre-freeze outcome → `FREEZE_B_OUTCOME_LEAKAGE`; missing stratum → `CALIBRATION_STRATUM_MISSING`; S1/K1 unlink → `SEARCH_CONFIG_LINK_MISMATCH`; R2 → `SEARCH_CONFIG_DOMAIN_MISMATCH`; CI gate → `SEARCH_CONFIG_GATE_LAW_MISMATCH`; capacity drift → `CALL_SCHEDULE_MISMATCH`; cap minus USD 0.01 → `BUDGET_CAP_INSUFFICIENT`. Then run `python -m pytest tests/test_phase12_filter_v5_scientific_laws.py::test_kappa_rejects_cross_task_pooling tests/test_phase12_filter_v5_scientific_laws.py::test_kappa_is_identical_across_all_candidate_classes tests/test_phase12_filter_v5_scientific_laws.py::test_kappa_excludes_candidate_absent_pairs tests/test_phase12_filter_v5_scientific_laws.py::test_screening_strict_does_not_rescue_current_control_wrong tests/test_phase12_filter_v5_scientific_laws.py::test_effective_yield_is_baseline_neutral_exposed_pair_rate tests/test_phase12_filter_v5_scientific_laws.py::test_unexposed_pair_reduces_effective_yield -q`; exact outcomes are `KAPPA_SCOPE_MISMATCH`, identical K1/K2 assessment for all four hidden classes, `assessment_state=not_evaluable`, `paired_evaluable=0/reason=CONTROL_NOT_CLEAN_SOLVABLE`, equal exposed yield `1.0` for fully evaluable one-/two-call baselines, and yield decrement `1/N` for one unexposed pair. Evidence `$ATTEMPT_DIR/task-4-*`.
  Pre-authorization evidence commands (run after the Task-4 QA commands and before requesting BCT authorization):
  ```bash
  test "$(realpath -m "$FILTER_V5_BCT_ARTIFACT_ROOT")" = "/home/hyunwoo/git/memory-contamination-diagnostic-filter-v5/runs/phase12-filter-v5-bct-live-v1" || { echo 'artifact root must be the repository-local live root' >&2; exit 2; }
  python scripts/build_phase12_filter_v5_bct_evidence.py --report freeze-b-search-config --bundle docs/evidence/phase12-filter-v5-bct-v1 --plan .omo/plans/phase12-post-filter-v5-calibration-readiness.md --artifact-root "$FILTER_V5_BCT_ARTIFACT_ROOT" --freeze-b data/phase12/filter_v5_bct_v1/freeze_b.json --authorization-request data/phase12/filter_v5_bct_v1/bct_authorization_request.json
  python scripts/verify_phase12_filter_v5_bct_evidence.py --through freeze-b --bundle docs/evidence/phase12-filter-v5-bct-v1 --plan .omo/plans/phase12-post-filter-v5-calibration-readiness.md --artifact-root "$FILTER_V5_BCT_ARTIFACT_ROOT"
  ```
  Commit: Y | `data(filter-v5): freeze bct search configuration`

- [x] 5. Execute, seal, and independently audit the four BCT families
  What to do / Must NOT do: Require external `BCTAuthorizationV1` plus matching `BCT_AUTH_EXPECTED_SHA256`, exact committed Freeze-B/SearchConfig identity, screening terminal seal and cumulative ledger head. If authorization is absent, emit `AWAITING_BCT_AUTHORIZATION` with zero client construction and route to Task 6. Otherwise execute new ID `filter-v5-bct-v1-attempt-001` for BCT-FV5-01..04 on frozen S2: attempt all 192 baseline/probe/candidate/replicate pairs and 384 isolated answer sides against a maximum 480 native method-call capacity; derive S1 without duplicate calls. Every native stage is `issued` or behaviorally `not_issued`; preserve model resistance, clean-control failure, FQ/FN, exposure/evaluability, ordinary-route and repeatability as behavior. Invalidate only on Scope infrastructure/cap/deadline violations. Never retry, reflect/write back, alter config, overwrite/resume, route Main memory, or select policy. In a fresh verifier process, reconcile raw public/audit streams rather than report flags: scheduled sides/stages, actual calls, answer-call provenance, tokens/retries/latency/cost, ledger reservations, no writeback, route invariance, all family/stratum coverage and permitted claims. Produce `bct_execution_report.json`, `archive_validation_report.json`, and `claim_scope_report.json`; validate all first eight reports as one backward-hash chain.
  Parallelization: Wave 5 | Blocked by: 4 | Blocks: 6
  References: [A3] `993–1218,1385–1518`; [A4] evaluation-law/no-pooling boundary `218–306,730–806`; [A8] `34–80,96–106`; [R1] Builder/Verifier and evidence trace; [R2] compute/result reporting; [I2] sealed evidence pattern; Task 2 archive/runner and Task 4 frozen inputs.
  Acceptance criteria: a successful run has manifest `status=completed`, exactly 192 attempted pairs/384 attempted answer sides, a complete issued/not-issued native-stage ledger, actual method calls ≤480, no retry or writeback, and BCT/shared consumption ≤480/570 calls, ≤USD 8/USD 10, ≤7,200/10,800 cumulative settled-or-reserved wall seconds, and exact token/deadline caps. Behavioral BoT downstream-stage skips remain completed evidence; infrastructure interruption/timeout maps to `status=invalidated` and preserves every settled row plus conservative call and wall-time reservations. Each settled pair has two explicit side relations and an immutable checkpoint. The three Task-5 reports bind the approved plan, authorities, freezes, SearchConfig, authorizations, ledger, raw ranges and archive seal; bounded capability claims are the only allowed claims.
  QA scenarios: Synthetic happy path: `env -u OPENAI_API_KEY python -m pytest tests/test_phase12_filter_v5_live_bct.py::test_valid_authorization_uses_injected_fake_factory_without_real_provider tests/test_phase12_filter_v5_live_bct.py::test_bot_distill_parse_failure_records_solve_not_issued_and_completed_archive -q`; assert 192 attempted pairs/384 sides, actual calls ≤480, and real-provider-factory count 0. Validate its scratch archive in a fresh process, build the three Task-5 reports, and run `python scripts/verify_phase12_filter_v5_bct_evidence.py --through bct --bundle docs/evidence/phase12-filter-v5-bct-v1 --plan .omo/plans/phase12-post-filter-v5-calibration-readiness.md --artifact-root "$FILTER_V5_BCT_ARTIFACT_ROOT"`; readiness validation remains reserved for F5 after Task 6 creates report 9. Run `python scripts/verify_phase12_filter_v5_bct_mutations.py --config configs/phase12/filter_v5_bct_calibration.yaml --freeze-b data/phase12/filter_v5_bct_v1/freeze_b.json --fixture-archive tests/fixtures/phase12/filter_v5/live/bct_completed_archive --output "$ATTEMPT_DIR/task-5-bct-mutations.json"`; require crash/timeout after pair 2 → invalidated append-only archive/two settled pairs/conservative reservation and terminal `FILTER_V5_PILOT_B_BLOCKED_BY_INVALID_BCT_EVIDENCE`, while public-label leak, missing MEB/reflexion coverage, native-stage drift, ledger-head drift, raw-range drift, answer-call mismatch and false-negative tamper each produce their named archive validation error and the same terminal block. Missing authorization emits `AWAITING_BCT_AUTHORIZATION`; expired/digest mismatch emits invalid-calibration/nonzero/factory 0. Only when external `$FILTER_V5_BCT_AUTH_PATH` and `$BCT_AUTH_EXPECTED_SHA256` are supplied may the worker run standalone `bct-run` with those external values; never pass a repository fixture to a production CLI. Evidence `$ATTEMPT_DIR/task-5-*` plus artifact root.
  Conditional BCT/evidence commands (run from repository root; repository fixtures are forbidden as authorization arguments):
  ```bash
  : "${FILTER_V5_BCT_ARTIFACT_ROOT:?set an authorized artifact root}"
  test "$(realpath -m "$FILTER_V5_BCT_ARTIFACT_ROOT")" = "/home/hyunwoo/git/memory-contamination-diagnostic-filter-v5/runs/phase12-filter-v5-bct-live-v1" || { echo 'artifact root must be the repository-local live root' >&2; exit 2; }
  if [ -n "${FILTER_V5_BCT_AUTH_PATH:-}" ] && [ -n "${BCT_AUTH_EXPECTED_SHA256:-}" ]; then
    python -m memcontam.cli phase12 filter-v5 bct-run --config configs/phase12/filter_v5_bct_calibration.yaml --freeze-b data/phase12/filter_v5_bct_v1/freeze_b.json --authorization-request data/phase12/filter_v5_bct_v1/bct_authorization_request.json --authorization "$FILTER_V5_BCT_AUTH_PATH" --expected-authorization-sha256 "$BCT_AUTH_EXPECTED_SHA256" --artifact-root "$FILTER_V5_BCT_ARTIFACT_ROOT" --run-id filter-v5-bct-v1-attempt-001 --stage-result "$FILTER_V5_BCT_ARTIFACT_ROOT/filter-v5-bct-v1-attempt-001-stage-result.json"
    python -m memcontam.cli phase12 filter-v5 validate-bct-archive --config configs/phase12/filter_v5_bct_calibration.yaml --freeze-b data/phase12/filter_v5_bct_v1/freeze_b.json --archive "$FILTER_V5_BCT_ARTIFACT_ROOT/filter-v5-bct-v1-attempt-001" --output "$FILTER_V5_BCT_ARTIFACT_ROOT/filter-v5-bct-v1-attempt-001/archive-validation-result.json"
  elif [ -z "${FILTER_V5_BCT_AUTH_PATH:-}" ] && [ -z "${BCT_AUTH_EXPECTED_SHA256:-}" ]; then
    python -m memcontam.cli phase12 filter-v5 bct-run --config configs/phase12/filter_v5_bct_calibration.yaml --freeze-b data/phase12/filter_v5_bct_v1/freeze_b.json --authorization-request data/phase12/filter_v5_bct_v1/bct_authorization_request.json --artifact-root "$FILTER_V5_BCT_ARTIFACT_ROOT" --run-id filter-v5-bct-v1-attempt-001 --stage-result "$FILTER_V5_BCT_ARTIFACT_ROOT/filter-v5-bct-v1-attempt-001-stage-result.json"
  else
    echo 'BCT authorization path and expected digest must be supplied together' >&2; exit 2
  fi
  python scripts/build_phase12_filter_v5_bct_evidence.py --report-set bct --bundle docs/evidence/phase12-filter-v5-bct-v1 --plan .omo/plans/phase12-post-filter-v5-calibration-readiness.md --artifact-root "$FILTER_V5_BCT_ARTIFACT_ROOT" --authorization-request data/phase12/filter_v5_bct_v1/bct_authorization_request.json --stage-result "$FILTER_V5_BCT_ARTIFACT_ROOT/filter-v5-bct-v1-attempt-001-stage-result.json"
  python scripts/verify_phase12_filter_v5_bct_evidence.py --through bct --bundle docs/evidence/phase12-filter-v5-bct-v1 --plan .omo/plans/phase12-post-filter-v5-calibration-readiness.md --artifact-root "$FILTER_V5_BCT_ARTIFACT_ROOT"
  ```
  Commit: Y | `docs(evidence): record filter-v5 bct calibration`

- [x] 6. Emit the Pilot-B stop gate and freeze code-source fidelity without execution
  What to do / Must NOT do: After Task 5 seals BCT evidence—or after Task 3/4 emits an earlier terminal branch—add immutable-authority-snapshot-bound inactive config `configs/phase12/exploratory_code_source_fidelity_v2.yaml` beside historical `exploratory_code.yaml`, with the six exact cell IDs, committed position-10 Game24 reservation, abstract slot, expected/max call table, `max_tool_rounds=1`, contrasts/diagnostics, sandbox digests, identities, no-pooling and fidelity labels from Scope. Validate `evidence_layer=build`, `scientific_result=false`, `activation_status=inactive`, `cost_cap_status=not_authorized_unpriced`, with no activation/resource/route/seed manifest, runner, provider/tool invocation, clean smoke, or historical-v1 change. `pilot-b-readiness` must accept the latest valid partial report chain (minimum reports 1–4), derive the first reachable terminal branch without requiring reports 5–9, and atomically write the stage result; `terminal-fill` then materializes missing reports 5–8 before report 9. Branch order is: no screening authorization → `AWAITING_SCREENING_AUTHORIZATION`; invalid screening/config/preview/pre-dispatch authorization → `FILTER_V5_PILOT_B_BLOCKED_BY_INVALID_CALIBRATION_EVIDENCE`; insufficient common-strict probes → `FILTER_V5_PILOT_B_NOT_ESTIMABLE`; valid Freeze B but no BCT authorization → `AWAITING_BCT_AUTHORIZATION`; attempted invalid/partial BCT archive → `FILTER_V5_PILOT_B_BLOCKED_BY_INVALID_BCT_EVIDENCE`; valid completed all-family BCT archive → `READY_FOR_SEPARATE_FILTER_V5_PILOT_B_AUTHORIZATION` regardless of behavioral failures. Emit every branch-conditional member of the nine-file chain and final `pilot_b_readiness_report.json`, create no SelectedPolicy, and expose no Pilot-B/Main execution.
  Parallelization: Wave 6, sequential | Blocked by: terminal output from 3/4/5 | Blocks: final verification
  References: [A1] `998–1010,1077–1216,1398–1597,1698–1800`; [A3] `1209–1409`; [A7] immutable §14.4 `1744–1822`; [A8] `61–106`; [S1] `91–159,362–487`; [S2] `19–30,199–292,681–842`; [S3] `25–35,64–137`; [S4] `26–83,193–219`; [I5] existing inactive code scaffold.
  Acceptance criteria: each synthetic branch fixture yields exactly one of the six prescribed terminal values with ordered machine-readable reasons and nine schema-valid reports; `terminal-fill` creates only unreachable reports 5–8 and never overwrites prior evidence. Ready is impossible unless all four BCT families are attempted under a valid completed archive; behavioral outcomes alone do not become infrastructure blockers. The ninth report binds prior eight report hashes and inactive code-prespec hash. Code v2 validates exact anchor/slot/sandbox/cell IDs, per-cell expected/max table totaling 12/13, `max_tool_rounds=1`, exact contrasts/metrics/labels, unpriced inactive status, provider/tool counters 0, and historical v1 unchanged; Pilot-B/Main call counters are 0 in every branch.
  QA scenarios: Run `python scripts/verify_phase12_filter_v5_readiness_fixtures.py --fixture-root tests/fixtures/phase12/filter_v5/readiness --code-prespec configs/phase12/exploratory_code_source_fidelity_v2.yaml --output-root "$ATTEMPT_DIR/task-6-readiness-fixtures"`; the script must execute the actual `pilot-b-readiness` CLI for exactly `awaiting-screening`, `invalid-calibration`, `not-estimable`, `awaiting-bct`, `invalid-bct`, and `ready`, and assert the matching terminal value, nine report files, and zero Pilot-B/Main calls. Then run `python -m pytest tests/test_phase12_filter_v5_pilot_b_readiness.py tests/test_code_exploratory_matrix.py::test_code_v2_call_table_totals_12_expected_13_max tests/test_tool_mode_no_pooling.py tests/test_phase12_canonical_configs.py tests/test_phase12_scientific_admission.py -q`; fixture parametrization must reject a prior-report hash change, partial-BCT ready claim, behavioral-FN-as-infrastructure relabel, SelectedPolicy/executable Pilot-B command, active/priced code status, call-table or `max_tool_rounds` drift, anchor/slot/sandbox/fidelity drift, and provider-native interpreter with zero provider/tool/Pilot-B/Main calls. Evidence `$ATTEMPT_DIR/task-6-*`.
  Final report commands:
  ```bash
  python -m memcontam.cli phase12 filter-v5 pilot-b-readiness --bundle docs/evidence/phase12-filter-v5-bct-v1 --code-prespec configs/phase12/exploratory_code_source_fidelity_v2.yaml --stage-result "$FILTER_V5_BCT_ARTIFACT_ROOT/pilot-b-readiness-stage-result.json"
  python scripts/build_phase12_filter_v5_bct_evidence.py --report-set terminal-fill --bundle docs/evidence/phase12-filter-v5-bct-v1 --plan .omo/plans/phase12-post-filter-v5-calibration-readiness.md --artifact-root "$FILTER_V5_BCT_ARTIFACT_ROOT" --stage-result "$FILTER_V5_BCT_ARTIFACT_ROOT/pilot-b-readiness-stage-result.json"
  python scripts/build_phase12_filter_v5_bct_evidence.py --report pilot-b-readiness --bundle docs/evidence/phase12-filter-v5-bct-v1 --plan .omo/plans/phase12-post-filter-v5-calibration-readiness.md --artifact-root "$FILTER_V5_BCT_ARTIFACT_ROOT" --stage-result "$FILTER_V5_BCT_ARTIFACT_ROOT/pilot-b-readiness-stage-result.json"
  python scripts/verify_phase12_filter_v5_bct_evidence.py --through readiness --bundle docs/evidence/phase12-filter-v5-bct-v1 --plan .omo/plans/phase12-post-filter-v5-calibration-readiness.md --artifact-root "$FILTER_V5_BCT_ARTIFACT_ROOT"
  ```
  Commit: Y | `docs(phase12): freeze inactive code study and pilot-b handoff`

## Final verification wave
> Runs in parallel after ALL todos. ALL must APPROVE. Surface results and wait for the user's explicit okay before declaring complete.
- [x] F1. Plan compliance audit
  Procedure: Map every Must-have/Must-NOT item, the applicable conditional branch, and all 6 tasks to committed repository code/config/data/docs, the immutable external-authority snapshot, raw/blocked archives, the exact nine-report bundle, and inactive code prespec. Rehash the descriptor-read plan, starting `HEAD:<path>` trees, sealed evidence, commits and [A3]–[A8]; prove no external theoretical-artifact path changed.
  Acceptance: emit `APPROVE` only if all applicable mappings pass, all skipped stages are justified by the terminal table, no task relied on synthetic scientific defaults, and no pending unrecorded exception exists. Output `$ATTEMPT_DIR/final-f1-plan-compliance.json`.
- [x] F2. Code quality review
  Procedure: Run the full Verification strategy gate, inspect changed Python for strict Pydantic models, typed errors, authorization-before-construction, descriptor-safe reads, crash-safe ledger operations, no secret logging, no duplicate framework, and ≤250 pure LOC per new module unless explicitly split. Review every changed production caller via CodeGraph/LSP.
  Acceptance: all applicable commands exit 0 and findings are empty; output `$ATTEMPT_DIR/final-f2-code-quality.json` with exact argv/commit/hashes. A missing paid authorization may skip only live calls, never local tests/replay/report verification.
- [x] F3. Real manual QA
  Procedure: Agent drives the actual CLI in fresh repository-local scratch root `$ATTEMPT_DIR/final-f3-scratch` after creating it and rejecting any symlink: zero-call config/preflight, absent/copied/expired/digest-swapped authorizations, duplicate run ID, concurrent fake ledger reservations, crash after dispatch intent, copied archive tamper, all six readiness branches, and—when external authorizations were supplied—the real screening/BCT archive validators. Inspect JSON/JSONL bytes and ledger heads, not only stdout.
  Acceptance: exact waiting/structural/invalid/ready and model-behavior distinctions hold; partial evidence and conservative reservations survive; every scheduled stage and settled call reconciles; no unauthorized provider/code/Pilot-B/Main execution. Output `$ATTEMPT_DIR/final-f3-real-qa.json`.
- [ ] F4. Scope fidelity
  Procedure: Compare exact starting commit `32efbe15fc645de84585acb55b78946cfcd4298a` through current `HEAD`, the ordered commit chain, immutable [A3]–[A8] hashes, historical v1 bytes, data/config/evidence layers, CLI help, provider/tool call ledgers, and claim text against Scope. Run `find -P "/home/hyunwoo/gdrive_undergrad_research/PeerJ fast-track/References/Theoretical Artifacts" -type f -print0 | sort -z | xargs -0 sha256sum > "$ATTEMPT_DIR/final-f4-external.sha256"` followed by `cmp "$ATTEMPT_DIR/task-1-external-before.sha256" "$ATTEMPT_DIR/final-f4-external.sha256"`.
  Acceptance: sealed v1 and all existing theoretical artifacts are byte-identical; only approved paths inside the code repository changed; Pilot-A/Main/SelectedPolicy/code activation absent; no paid call references an uncommitted freeze; final worktree clean. Output `$ATTEMPT_DIR/final-f4-scope-fidelity.json`.

- [ ] F5. Fresh evidence-bundle reconciliation
  Procedure: From a fresh process, run `python scripts/verify_phase12_filter_v5_bct_evidence.py --through readiness --bundle docs/evidence/phase12-filter-v5-bct-v1 --plan .omo/plans/phase12-post-filter-v5-calibration-readiness.md --artifact-root "$FILTER_V5_BCT_ARTIFACT_ROOT"`. The verifier must descriptor-open/no-follow and rehash the live plan, all nine reports, authority/config/freeze inputs, authorization receipts, ledger chain, manifests, seals and every referenced raw byte range; it must recompute report conclusions without trusting stored `all_passed`, status or aggregate fields.
  Acceptance: emit `APPROVE` only when the live plan digest equals the externally approved digest, the nine-report chain is complete/current, every branch result is recomputed, and no stale or self-authenticating success field can pass. Mutating any source after report creation must change the verdict to rejection. Output `$ATTEMPT_DIR/final-f5-evidence-reconciliation.json`.

## Commit strategy
- Use exactly eight task-scoped conventional commits when the full authorized path runs; an earlier terminal branch omits only unreachable downstream evidence commits. Python implementation and direct tests stay together.
- Exact full-path order: (1) immutable-authority/Methods validators, (2) repository authority snapshot + Methods inputs, (3) minimal live seam plus evidence producer/validator, (4) Freeze A plus reports 1–3, (5) screening report 4, (6) Freeze B/SearchConfig/request plus report 5, (7) BCT reports 6–8, (8) inactive code prespec plus readiness report 9/Pilot-B handoff.
- Freeze A must be committed before screening authorization/dispatch; Freeze B/SearchConfig must be committed before BCT authorization/dispatch. Never amend/squash any commit referenced by a paid call; any code/config change invalidates downstream authorization and requires a new freeze/run ID.
- Raw provider archives live under the authorized artifact root. Commit only the nine redacted/hash-bound evidence reports; never commit secrets, raw API credentials, or unrestricted audit labels.
- Do not append the prohibited commit footers in repository `AGENTS.md`.

## Success criteria
- Execution began from exact clean main baseline and sealed v1 source/evidence bytes remain unchanged.
- The user-approved repository-local plan descriptor digest matches this plan and is identical across Methods, authorizations, freezes, SearchConfig, raw BCT archive, and nine evidence reports.
- [A3]–[A8] match their accepted immutable hashes; the repository transition manifest records `external_files_changed=false`, closes consumer-binding provenance without retroactive rebinding, and no existing theoretical artifact is edited.
- Methods lock contains the exact user-approved conservative grid, all 12 strata, Pilot-A provider identity, and USD 10 shared budget.
- Freeze A contains 18 new certified/disjoint candidate-absent probes, six exact source/construction manifests, 36 candidate renders, 12 mature checkpoints and 12 ordinary-writer realizations; screening uses no candidate/challenge call and is fully archived, canonically invalidated, or waiting before client construction.
- Freeze B contains exactly two common-strict probes per task or records structural not-estimability; SearchConfig contains exactly 16 hash-bound candidates and no selected policy.
- Authorization is external, descriptor/digest/time/scope/hash/model/run/cap/ledger-bound; absence or drift yields zero provider calls. Crash-safe shared call/token/cost/wall-time caps cannot be exceeded or silently released across processes/attempts.
- When authorized, screening represents all 72 scheduled controls with ≤90 actual method calls and BCT attempts all 192 pairs/384 sides with ≤480 actual method calls only on frozen S2; both reconcile every issued/not-issued native stage, derive S1 without duplicate calls, preserve model failures/not-evaluable outcomes, and never write back or enter ordinary denominators.
- Canonical completed/invalidated/rerun archives preserve all settled records and reconcile calls, provenance, tokens, retries, latency, cost, wall-time reservations/settlements, byte ranges, manifests and seals with public/audit separation.
- The exact nine-file evidence bundle is freshly recomputed, supports only bounded registered capability claims, and ends in one terminal-table value; no Pilot-B/Main/SelectedPolicy exists.
- Code source-fidelity v2 is immutable-authority-snapshot-bound, six-cell/one-slot, 12 expected/13 maximum calls with one tool round, unpriced, inactive, no-pooling, and provider/tool invocation count is zero.
- Full tests, Ruff, mypy, replay run+aggregate gate, focused live/authorization/archive mutations, and five final verifiers pass; final worktree is clean.
